import Foundation

// Used for testing NoteView preview.
extension NoteData {
    static var sampleMockData: [NoteData]{
        [
            NoteData(notePosition: 0, textData: TextData(text: "I am an amazing text Data wuuu"))
        ]
    }
}
